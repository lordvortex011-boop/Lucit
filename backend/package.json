{
  "name": "lucit-backend",
  "version": "0.0.0",
  "private": true,
  "description": "Reserved for a separate backend service. Empty for now — nothing implemented.",
  "scripts": {
    "dev": "echo \"backend not implemented yet\" && exit 0"
  }
}
